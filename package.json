{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs = require('fs'); ['package-lock.json', 'yarn.lock'].forEach(f => { if (fs.existsSync(f)) fs.unlinkSync(f) }); if (!process.env.npm_config_user_agent || !process.env.npm_config_user_agent.startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "pnpm": "corepack pnpm",
    "dev": "corepack pnpm --parallel --filter @workspace/api-server --filter @workspace/aaa-bets run dev",
    "dev:api": "corepack pnpm --filter @workspace/api-server run dev",
    "dev:web": "corepack pnpm --filter @workspace/aaa-bets run dev",
    "start": "corepack pnpm --filter @workspace/api-server run start",
    "railway:build": "corepack pnpm build",
    "railway:start": "corepack pnpm start",
    "codegen": "corepack pnpm --filter @workspace/api-spec run codegen",
    "build": "corepack pnpm run typecheck && corepack pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
